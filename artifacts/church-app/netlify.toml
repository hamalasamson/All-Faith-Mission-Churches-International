[build]
  base    = "."
  command = "pnpm --filter @workspace/church-app run build:netlify"
  publish = "artifacts/church-app/dist"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
  PNPM_VERSION = "9"
